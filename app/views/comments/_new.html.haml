=render 'common/errors', model: @comment
= form_for [@movie, @comment ], url:movie_comments_path(@movie, @comment) do |f|
  %form
    .form-group
    = f.label :body
    %br
    = f.text_area :body
    .form-group
      =f.button :submit, class: "btn btn-primary"
